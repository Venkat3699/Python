import sys
type = sys.argv[1]

if type == "t2.micro":
    print("ok, we will create an instance as t2.micro")

# Command and Output: 
# python Basic.py t2.micro
# Output: ok, we will create an instance as t2.micro